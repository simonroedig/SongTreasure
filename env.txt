// follow the instructions (the // line precisely)
// this file holds environment variables that will be loaded into the source code
// keep the first 4 environment variables as they are, don't replace them
// replace the values of the last 2 environment variables
// rename file to solely: .env
// delete all "//" lines 

SECRET_KEY="rZjPqV47tmxihd9OSWqu24bJNxlbgfo7"
MODE="DEV"
REDIRECT_URI_DEV="http://localhost:8080/callback"
REDIRECT_URI="you don't need to replace this, it is for possible hosting in the future"

SPOTIFY_CLIENT_ID="REPLACE"
SPOTIFY_CLIENT_SECRET="REPLACE"